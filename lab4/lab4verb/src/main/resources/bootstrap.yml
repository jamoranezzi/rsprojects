---
spring:
  profiles:
    active: verb # # Solamente por la prueba paara indicar el profile que se esta utilizando.
  application:
    name: lab-4-verb
  cloud:
    config:
      uri: http://localhost:8001