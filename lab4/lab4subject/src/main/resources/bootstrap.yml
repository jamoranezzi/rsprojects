---
spring:
  profiles:
    active: subject # Solamente por la prueba paara indicar el profile que se esta utilizando.
  application:
    name: lab-4-subject
  cloud:
    config:
      uri: http://localhost:8001